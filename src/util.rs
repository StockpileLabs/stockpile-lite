#[cfg(test)]
mod test {
    use super::*;
    use std::collections::BTreeMap;

    #[test]
    fn try_insert_into_table() -> Result<()> {
        Ok(())
    }

    #[test]
    fn try_update_shares() -> Result<()> {
        Ok(())
    }

    #[test]
    fn try_admit_participant() -> Result<()> {
        Ok(())
    }
}